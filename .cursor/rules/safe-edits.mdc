---
description: Cursor adapter - follow AGENTS.md (safe edits)
globs: src/**/*
---

# Safe edits (adapter)

**Primary source:** [`AGENTS.md`](../../AGENTS.md).

- No secrets (`.env`, tokens, keys)
- Do not invent top-level layers beyond existing folders
- Prefer Plop for new features/components
- Run `test` / `lint` / `type-check` before claiming done
- Never break the Dependency Rule in `AGENTS.md`
